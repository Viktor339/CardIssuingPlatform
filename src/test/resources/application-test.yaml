
spring:
  liquibase:
    enabled: true
    change-log: classpath:changelog/sql/db.changelog-master.yaml
    contexts: dev
  jpa:
    show-sql: true
validator:
  company:
    pageSize:
      min: 1
      max: 20
